function content(html){document.querySelector('#content').innerHTML=html}
function stat(label,val,sub=''){return `<div class="card"><div class="label">${label}</div><div class="value">${val}</div><div class="sub">${sub}</div></div>`}
function marketCard(k){let a=ASSETS[k];return `<div class="card"><div class="asset-head"><img class="coin-logo" src="assets/crypto/${a.icon}.svg"><div><div class="asset-name">${a.name}</div><div class="asset-symbol">${k} · ${a.major?'Major':'Meme'}</div></div><span class="badge" style="margin-left:auto">LIVE</span></div><div class="price">${money(a.price)}</div><div class="${a.change>=0?'up':'down'}" style="font-weight:800">${a.change>=0?'+':''}${a.change.toFixed(2)}%</div><div class="mini"><canvas id="mini-${k}"></canvas></div><div class="trade-row"><button class="btn buy" onclick="trade('${k}','buy')">BUY</button><button class="btn sell" onclick="trade('${k}','sell')">SELL</button></div></div>`}
function renderDashboard(){let tv=portfolioValue(),inv=invested(),pnl=tv-state.cash-inv;content(`<div class="page"><div class="hero"><div><div class="eyebrow">OVERVIEW / LIVE ENGINE</div><h2>Good trading.</h2><p>Your futuristic KEREOS command center.</p></div><button class="btn primary" onclick="location.href='markets.html'">Trade crypto →</button></div><div class="cards">${stat('Total portfolio',money(tv),'Live valuation')}${stat('Cash available',money(state.cash),'Ready to trade')}${stat('Invested',money(inv),'Current cost basis')}${stat('Unrealized P&L',(pnl>=0?'+':'')+money(pnl),pnl>=0?'Profit':'Loss')}</div><div class="two panel"><div class="card"><div class="panel-head"><h3>Portfolio performance</h3><span class="badge">LIVE</span></div><div class="chart-wrap"><canvas id="portfolioChart"></canvas></div></div><div class="card"><div class="panel-head"><h3>Quick actions</h3></div><button class="btn primary" style="width:100%;margin-bottom:9px" onclick="location.href='markets.html'">＋ Trade crypto</button><button class="btn" style="width:100%;margin-bottom:9px" onclick="location.href='portfolio.html'">Open portfolio</button><button class="btn" style="width:100%" onclick="location.href='settings.html'">Settings</button><div class="sub" style="margin-top:18px">Live demo simulation. Prices move automatically.</div></div></div><div class="panel"><div class="panel-head"><h3>Popular markets</h3><button class="btn" onclick="location.href='markets.html'">View all</button></div><div class="market-grid">${['BTC','ETH','SOL','XRP'].map(marketCard).join('')}</div></div></div>`);drawPortfolio();['BTC','ETH','SOL','XRP'].forEach(k=>chart(document.querySelector('#mini-'+k),k))}
function drawPortfolio(){let c=document.querySelector('#portfolioChart');if(!c)return;let h=state.history.map(x=>x.v);if(h.length<5)h=Array.from({length:50},(_,i)=>10000+i*Math.sin(i/4)*40);canvasLine(c,h,'#9b6cff')}
function renderMarkets(){content(`<div class="page"><div class="hero"><div><div class="eyebrow">MARKETS / LIVE FEED</div><h2>Explore markets.</h2><p>Green/red candles, live simulation and exact trade markers.</p></div><div class="pill">● LIVE SIMULATION</div></div><input id="search" placeholder="⌕  Search BTC, ETH, PEPE..." style="width:100%;margin-bottom:16px" oninput="filterMarkets()"><div class="market-grid" id="marketGrid">${Object.keys(ASSETS).map(marketCard).join('')}</div></div>`);Object.keys(ASSETS).forEach(k=>candles(document.querySelector('#mini-'+k),k))}
function filterMarkets(){let q=document.querySelector('#search').value.toLowerCase();document.querySelectorAll('#marketGrid .card').forEach((el,i)=>el.style.display=Object.keys(ASSETS)[i].includes(q.toUpperCase())||ASSETS[Object.keys(ASSETS)[i]].name.toLowerCase().includes(q)?'':'none')}
function renderPortfolio(){let rows=Object.entries(state.positions);let tv=portfolioValue(),inv=invested(),pnl=tv-state.cash-inv;content(`<div class="page"><div class="hero"><div><div class="eyebrow">PORTFOLIO / CONTROL CENTER</div><h2>Your positions.</h2><p>Every fill updates quantity, average price, cash and P&L.</p></div><button class="btn" onclick="location.href='markets.html'">Trade →</button></div><div class="cards">${stat('Total value',money(tv),'Cash + positions')}${stat('Cash',money(state.cash),'Available')}${stat('Invested',money(inv),'Cost basis')}${stat('P&L',(pnl>=0?'+':'')+money(pnl),pnl>=0?'Profit':'Loss')}</div><div class="card panel"><div class="panel-head"><h3>Holdings</h3><span class="badge">LIVE MARK-TO-MARKET</span></div>${rows.length?`<div class="table-wrap"><table class="table"><thead><tr><th>Asset</th><th>Quantity</th><th>Avg price</th><th>Mark</th><th>Value</th><th>P&L</th><th></th></tr></thead><tbody>${rows.map(([k,p])=>{let val=p.qty*ASSETS[k].price,gain=val-p.qty*p.avg;return `<tr><td><div class="asset-head"><img class="coin-logo" style="width:34px;height:34px" src="assets/crypto/${ASSETS[k].icon}.svg"><b>${k}</b></div></td><td>${p.qty.toFixed(8)}</td><td>${money(p.avg)}</td><td>${money(ASSETS[k].price)}</td><td>${money(val)}</td><td class="${gain>=0?'up':'down'}">${gain>=0?'+':''}${money(gain)}</td><td><button class="btn sell" onclick="trade('${k}','sell')">Sell</button></td></tr>`}).join('')}</tbody></table></div>`:'<div class="empty">No positions yet. Trade an asset in Markets.</div>'}</div><div class="two panel"><div class="card"><div class="panel-head"><h3>Portfolio performance</h3></div><div class="chart-wrap"><canvas id="portfolioChart"></canvas></div></div><div class="card"><h3>Allocation</h3>${rows.map(([k,p])=>`<div style="margin:15px 0"><div style="display:flex;justify-content:space-between"><span>${k}</span><b>${(p.qty*ASSETS[k].price/tv*100).toFixed(1)}%</b></div><div class="progress"><i style="width:${Math.min(100,p.qty*ASSETS[k].price/tv*100)}%"></i></div></div>`).join('')||'<div class="empty">Your allocation appears after your first trade.</div>'}</div></div></div>`);drawPortfolio()}
function renderOrders(){content(`<div class="page"><div class="hero"><div><div class="eyebrow">ORDERS / EXECUTION LOG</div><h2>Order flow.</h2><p>Every executed demo trade is recorded here.</p></div></div><div class="card"><div class="table-wrap"><table class="table"><thead><tr><th>ID</th><th>Time</th><th>Asset</th><th>Side</th><th>Price</th><th>Quantity</th><th>Value</th></tr></thead><tbody>${state.orders.length?state.orders.map(o=>`<tr><td>${o.id}</td><td>${o.time}</td><td><b>${o.asset}</b></td><td class="${o.side==='BUY'?'up':'down'}"><b>${o.side}</b></td><td>${money(o.price)}</td><td>${o.qty.toFixed(8)}</td><td>${money(o.value)}</td></tr>`).join(''):'<tr><td colspan="7" class="empty">No executed orders yet. Your trades will appear here.</td></tr>'}</tbody></table></div></div></div>`)}
function renderWatchlist(){content(`<div class="page"><div class="hero"><div><div class="eyebrow">WATCHLIST / SIGNALS</div><h2>Keep an eye.</h2><p>Tap any market to open its trade panel.</p></div><button class="btn primary" onclick="location.href='markets.html'">＋ Add market</button></div><div class="card">${state.watch.map(k=>`<div class="setting" style="margin-bottom:9px"><div class="asset-head"><img class="coin-logo" src="assets/crypto/${ASSETS[k].icon}.svg"><div><b>${ASSETS[k].name}</b><div class="asset-symbol">${k} · ${ASSETS[k].major?'Major':'Meme'}</div></div></div><div><b>${money(ASSETS[k].price)}</b> <span class="${ASSETS[k].change>=0?'up':'down'}">${ASSETS[k].change>=0?'+':''}${ASSETS[k].change.toFixed(2)}%</span> <button class="btn" onclick="trade('${k}','buy')">Trade</button></div></div>`).join('')}</div></div>`)}
function renderAnalytics(){let tv=portfolioValue(),inv=invested();content(`<div class="page"><div class="hero"><div><div class="eyebrow">ANALYTICS / PERFORMANCE</div><h2>Know your edge.</h2><p>Simple metrics from your demo trading activity.</p></div></div><div class="cards">${stat('Total return',((tv-10000)/10000*100).toFixed(2)+'%','Since reset')}${stat('Executed orders',state.orders.length,'Filled trades')}${stat('Win rate',state.orders.length?((state.orders.filter(o=>o.side==='SELL').length/state.orders.length)*100).toFixed(0)+'%':'0%','Demo metric')}${stat('Capital deployed',money(inv),'Current cost basis')}</div><div class="two panel"><div class="card"><h3>Equity curve</h3><div class="chart-wrap"><canvas id="portfolioChart"></canvas></div></div><div class="card"><h3>Risk snapshot</h3><p class="sub">Cash buffer</p><div class="value">${(state.cash/tv*100).toFixed(1)}%</div><p class="sub">Largest position</p><div class="value">${Object.entries(state.positions).sort((a,b)=>b[1].qty*ASSETS[b[0]].price-a[1].qty*ASSETS[a[0]].price)[0]?.[0]||'—'}</div></div></div></div>`);drawPortfolio()}
function renderChallenges(){content(`<div class="page"><div class="hero"><div><div class="eyebrow">CHALLENGES / LEVEL UP</div><h2>Level up.</h2><p>Trade carefully, learn the terminal and beat your demo goals.</p></div></div><div class="three">${[['FIRST FILL','Make your first demo trade.',state.orders.length>0],['BUILD A POSITION','Hold any asset with a positive mark.',Object.keys(state.positions).length>0],['FULL LOOP','Complete a buy and a sell.',state.orders.some(o=>o.side==='BUY')&&state.orders.some(o=>o.side==='SELL')]].map(x=>`<div class="card"><div style="font-size:26px">${x[2]?'✓':'ϟ'}</div><h3>${x[0]}</h3><p class="sub">${x[1]}</p><div class="progress"><i style="width:${x[2]?100:35}%"></i></div><div class="sub">${x[2]?'COMPLETED':'IN PROGRESS'}</div></div>`).join('')}</div></div>`)}
function renderNews(){content(`<div class="page"><div class="hero"><div><div class="eyebrow">NEWS / MARKET BRIEF</div><h2>Market intelligence.</h2><p>Demo market intelligence cards—no external feed required.</p></div></div><div class="news-grid">${[['Crypto liquidity monitor','Watch volatility and position size as simulated prices move.'],['Execution engine online','Orders update cash, quantity, average entry and portfolio value instantly.'],['V13 terminal upgrade','Candles, trade markers, themes, sounds and responsive mobile layout are connected.']].map(x=>`<div class="card news-card"><div class="eyebrow">KEREOS</div><h3>${x[0]}</h3><p class="sub">${x[1]}</p></div>`).join('')}</div></div>`)}
function renderSettings(){content(`<div class="page"><div class="hero"><div><div class="eyebrow">SETTINGS / TERMINAL</div><h2>Make it yours.</h2><p>Control appearance, sound, animation and currency.</p></div></div><div class="settings-grid"><div class="setting"><div><b>Theme</b><div class="sub">Dark or light terminal</div></div><select onchange="setTheme(this.value)"><option value="dark" ${state.theme==='dark'?'selected':''}>Dark</option><option value="light" ${state.theme==='light'?'selected':''}>Light</option></select></div><div class="setting"><div><b>Sound effects</b><div class="sub">Buy/sell confirmation sounds</div></div><button class="switch ${state.sound?'on':''}" onclick="toggleSetting('sound')"><i></i></button></div><div class="setting"><div><b>Live animation</b><div class="sub">Moving background and market simulation</div></div><button class="switch ${state.animation?'on':''}" onclick="toggleSetting('animation')"><i></i></button></div><div class="setting"><div><b>Currency</b><div class="sub">Display values in your preferred currency</div></div><select onchange="setCurrency(this.value)"><option ${state.currency==='USD'?'selected':''}>USD</option><option ${state.currency==='INR'?'selected':''}>INR</option><option ${state.currency==='EUR'?'selected':''}>EUR</option><option ${state.currency==='GBP'?'selected':''}>GBP</option></select></div><div class="card" style="grid-column:1/-1"><h3>Demo account</h3><p class="sub">Resetting clears positions and order history.</p><button class="btn sell" onclick="resetDemo()">Reset demo account</button></div></div></div>`)}
function setTheme(t){state.theme=t;save();applyTheme();window.pageRender?.()}
function applyTheme(){document.body.dataset.theme=state.theme;document.documentElement.style.setProperty('--bg',state.theme==='light'?'#f5f3fb':'#070711');document.documentElement.style.setProperty('--panel',state.theme==='light'?'#fff':'#111020');if(state.theme==='light'){document.documentElement.style.setProperty('--text','#171322');document.documentElement.style.setProperty('--muted','#686176');document.documentElement.style.setProperty('--line','rgba(20,15,40,.12)')}else{document.documentElement.style.setProperty('--text','#f7f5ff');document.documentElement.style.setProperty('--muted','#a9a4bc');document.documentElement.style.setProperty('--line','rgba(255,255,255,.09)')}document.querySelector('.grid')?.style.setProperty('opacity',state.animation?'.15':'0')}
function toggleSetting(k){state[k]=!state[k];save();applyTheme();window.pageRender?.()}
function resetDemo(){if(confirm('Reset the KEREOS demo account?')){state={cash:10000,positions:{},orders:[],watch:['BTC','ETH','SOL'],history:[],currency:state.currency,theme:state.theme,sound:state.sound,animation:state.animation};save();toast('Demo account reset');window.pageRender?.()}}
function initPage(){shell();applyTheme();let file=location.pathname.split('/').pop()||'index.html';let map={'index.html':renderDashboard,'markets.html':renderMarkets,'portfolio.html':renderPortfolio,'orders.html':renderOrders,'watchlist.html':renderWatchlist,'analytics.html':renderAnalytics,'challenges.html':renderChallenges,'news.html':renderNews,'settings.html':renderSettings};window.pageRender=map[file]||renderDashboard;window.pageRender()}
document.addEventListener('DOMContentLoaded',initPage);
